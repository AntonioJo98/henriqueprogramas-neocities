export const createLibrary=()=>({books:[],nextId:1});export function addBook(s,v){if(!v.title?.trim()||!v.author?.trim())return s;return{books:[...s.books,{id:s.nextId,title:v.title.trim(),author:v.author.trim(),status:v.status||'To read'}],nextId:s.nextId+1}}export function updateBook(s,id,v){return{...s,books:s.books.map(b=>b.id===id?{...b,...v}:b)}}export function removeBook(s,id){return{...s,books:s.books.filter(b=>b.id!==id)}}export function filterBooks(books,{query='',status=''}={}){const q=query.toLowerCase();return books.filter(b=>(!q||`${b.title} ${b.author}`.toLowerCase().includes(q))&&(!status||b.status===status))}