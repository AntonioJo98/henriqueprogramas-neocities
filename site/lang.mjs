const key='henrique-language';const pt=new Map([...document.querySelectorAll('[data-en]')].map(e=>[e,e.textContent]));function setLanguage(lang){document.documentElement.lang=lang;document.querySelectorAll('[data-en]').forEach(e=>e.textContent=lang==='en'?e.dataset.en:pt.get(e));document.querySelectorAll('.language-toggle').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.language===lang)));localStorage.setItem(key,lang)}document.querySelectorAll('.language-toggle').forEach(b=>b.addEventListener('click',()=>setLanguage(b.dataset.language)));setLanguage(localStorage.getItem(key)||'pt-PT');