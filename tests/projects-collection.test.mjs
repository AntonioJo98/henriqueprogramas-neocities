import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('JavaScript collection presents eight interactive project cards',async()=>{const h=await readFile(new URL('../site/projects/index.html',import.meta.url),'utf8');const cards=[...h.matchAll(/data-project="([^"]+)"/g)].map(m=>m[1]);assert.deepEqual(cards,['snake','turtle-crossing','flash-cards','coffee-wifi','whack-a-mole','calculator','disappearing-text','virtual-bookshelf']);assert.equal((h.match(/class="project-card project-card--live"/g)||[]).length,8);for(const path of['snake','turtle-crossing','flash-cards','coffee-wifi','whack-a-mole','calculator','disappearing-text','virtual-bookshelf'])assert.match(h,new RegExp(`href="/projects/${path}/"`))});