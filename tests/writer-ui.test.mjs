import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('writer page has editor status and reset',async()=>{const h=await readFile(new URL('../site/projects/disappearing-text/index.html',import.meta.url),'utf8');assert.match(h,/id="editor"/);assert.match(h,/id="writer-status"/);assert.match(h,/id="reset-button"/);assert.match(h,/writer\.mjs/)})