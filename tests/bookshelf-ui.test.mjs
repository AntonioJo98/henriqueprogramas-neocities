import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('bookshelf page exposes add form filters and list',async()=>{const h=await readFile(new URL('../site/projects/virtual-bookshelf/index.html',import.meta.url),'utf8');for(const id of['book-form','book-title','book-author','book-status','book-query','books-list'])assert.match(h,new RegExp(`id="${id}"`));assert.match(h,/bookshelf\.mjs/)})