import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';const root=new URL('../site/projects/calculator/',import.meta.url);test('calculator exposes display, history and keypad',async()=>{const html=await readFile(new URL('index.html',root),'utf8');assert.match(html,/id="display"/);assert.match(html,/id="history"/);assert.match(html,/data-key="="/);assert.match(html,/aria-live="polite"/);assert.match(html,/calculator\.mjs/)});